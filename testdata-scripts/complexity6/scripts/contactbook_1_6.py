
def add_contact(name, number):
    contacts = []
    contacts.append({'name': name, 'number': number})  # Adds contact to a local list

add_contact('Jane Doe', 'not-a-number')
print(contacts)
